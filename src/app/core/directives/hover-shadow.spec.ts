import { HoverShadow } from './hover-shadow';

describe('HoverShadow', () => {
  it('should create an instance', () => {
    const directive = new HoverShadow();
    expect(directive).toBeTruthy();
  });
});
